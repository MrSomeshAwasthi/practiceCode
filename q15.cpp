/*
Given a sequence of n numbers, representing the stock prices of a stock on different
days, design a linear time algorithm to compute the maximum profit that you can
make by buying and selling a stock exactly once, you can sell a stock exactly k days
after you bought it.
*/
#include<bits/stdc++.h>
using namespace std;
int max_profit(vector<int> vec)
{
    
    return 0;
}
int main()
{   
    vector <int > vec={100, 180, 260, 310, 40, 535, 695};
    max_profit(vec);
    return 0;
}